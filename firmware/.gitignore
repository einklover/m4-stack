**/__pycache__/
*.pyc
.pio/
artifacts/fonts/release/*.epdfont
artifacts/fonts/source/
backups/
build/
tools/preview_output/
artifacts/
# Local IDE / runtime (do not commit)
compile_commands.json
sdcard/
.DS_Store
# m4adb local cache / serial captures (never commit packages or secrets)
build/m4adb/
*.session.json
**/session.json
simulator/build*/

# Vendored FreeInk SDK + third-party trees live in-tree (open-m4-sdk, Epub, Lua,
# expat, miniz, picojpeg, EpdFont/builtinFonts). Do not re-ignore them.
