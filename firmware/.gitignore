**/__pycache__/
*.pyc
.pio/
artifacts/fonts/release/*.epdfont
artifacts/fonts/source/
backups/
build/
artifacts/
# Local IDE / runtime (do not commit)
compile_commands.json
sdcard/
.DS_Store
# m4adb local cache / serial captures (never commit packages or secrets)
build/m4adb/
*.session.json
**/session.json
simulator/build*/

# Generated font headers from a user-supplied TTF (never commit).
lib/EpdFont/builtinFonts
